# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/ims_db

# JWT config (replace secret with a secure env var in production)
app.jwt.secret=change_this_to_a_long_random_secret_at_least_32_chars
app.jwt.expirationMs=86400000

# Allow CORS origins if needed (optional)
frontend.origin=http://localhost:5173